---
layout: template
---

<build-deck
  webc:nokeep
  id="my-very-good-talk"
  :@slides="this.slideDeck"
>
  <default-slide
    webc:nokeep
    :@slide="({
      pre: 'inserted',
      title: '**before** slide loop',
      caption: 'Using the `<build-deck>` default `<slot>`',
    })"
  ></default-slide>

  <div slot="after" webc:nokeep>
    <default-slide :@slide="({
      pre: 'inserted',
      title: '**after** slide loop',
      caption: 'Using the `<build-deck>` `<slot name=after>`',
    })"></default-slide>
  </div>

  <slide-controls
    webc:nokeep
    slot="controls"
  >
    <h2>Theming (extra controls)</h2>
    <label for="color-scheme">Color Scheme</label>
    <select id="color-scheme">
      <option value="auto-mode">Auto</option>
      <option value="light-mode">Light</option>
      <option value="dark-mode">Dark</option>
    </select>
  </slide-controls>
</build-deck>

<style>
  html {
    block-size: 100%;
    scroll-snap-type: block proximity;
    text-size-adjust: none;
  }

  body {
    font-size: clamp(1em, 0.8em + 0.5vi, 2em);
    margin: unset;
    min-block-size: 100%;
  }

  event-caption {
    align-items: center;
    justify-items: start;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: var(--slide-gap);
  }

  .oddlogo {
    width: auto;
    height: 1.5lh;
  }
</style>
